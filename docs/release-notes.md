# Release information

[Current state](current-state.md) owns deployed identity, download locations,
qualification scope and open limitations.

0909 packages the current lab source around accepted native inputs; production
still uses its existing 0908 VM. Hashes, exact runtime/source identity and dated
acceptance records belong alongside immutable release artifacts, not in a
second growing chronology.

For older findings, use `git log --all -- docs reference`. Old reports are
evidence of their recorded run, not instructions or proof for the current lab.
